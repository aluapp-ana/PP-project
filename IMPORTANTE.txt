1. A partir de agora, apenas as imagens com keypoints deverão ser extraídas,
ou seja, não usares mais .csv ou .pkl.

2. Fazer script (move) para reorganização do dataset MINDS, onde deverão ser separados
por labels(palavras) e não por sinalizadores(pessoa).

    Exemplo: 
                Como está: pasta do sinalizador 1 > todos os videos do sinalizador 1 fazendo os sinais 
                            de todas as 20 palavras em libras.

                Como deve ficar: pasta label Banana > sinalizadores que fizeram o sinal em libras da 
                                palavra banana. 